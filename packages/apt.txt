# Shell tools
bat
eza
fd-find
ripgrep
zoxide

# Secrets management
age

# Development
htop
yq

# Build tools (treesitter parser compilation) and font install
build-essential
unzip

# NOTE: tools that apt ships too old or not at all (starship, fzf, sops,
# lazygit, git-delta, tree-sitter, uv) are installed via mise instead;
# see packages/mise.txt. neovim is installed via packages/nvim.sh.
# packages/install.sh handles all of this automatically.
